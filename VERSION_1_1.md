# Version 1.1 — Next Tasks

> Compiled 2026-07-04 against **v0.127.0**. Eighty candidate tasks — ten per area — each proposed from a read of the live code/specs and then **verified against the codebase** (already-shipped and too-vague proposals were rejected and replaced). Items are in rough priority order within each area. Tags: **FIX** = broken or misleading today · **IMPROVE** = sharpen an existing feature · **OVERHAUL** = redo a system · **NEW** = net-new.
>
> Working agreement carries over from 1.0: one gated unit per item (gate green + negative control + CHANGELOG + BROWSER_QA entry where visual/audio), spec edits by versioned copy, no bugs ship.

## Intro Cinematic and Menu Screen

1. **Persist Continue across reloads (profile.lastGame)** · `FIX`
   Verified: this.lastGameId is in-memory only (starnix-shell.js line 36), set solely in enterGame at line 1239, never persisted — so on every fresh boot both the dock CTA (lines 537-546) and the redundant top 'Continue' button (line 550) vanish even though G2 saves sit in profile.saves. Write profile.lastGame in enterGame right where lastGameId is set (persist via core.persistence.save, same pattern as rankSeen at lines 587-589), read it in showMenu with a fallback to any populated profile.saves entry, and dedupe the two Continue buttons into the dock CTA only. Note the interplay: Continue into a game with a save correctly lands on the existing Resume/New-game panel (~line 1210), so no extra wiring is needed there. Pin the persistence round-trip in verify-build.

2. **Promote the due-review chip out of the ghost-button row** · `IMPROVE`
   Verified: the '⏰ N due · Review' chip (starnix-shell.js lines 552-557) is a .sx-btn-ghost with only a gold border/text (CSS lines 1500-1501), sitting among Settings/Codex/Replay-intro in the top-right — the highest-learning-value CTA in the app styled as tertiary chrome. When dueQs.length > 0, render it instead as a gold banner in the bridge dock next to the DAILY MISSIONS label (line 492), escalating to a full-width strip above .sx-cards when count ≥ 10. Keep the .sx-due-chip behavioral class so the existing wiring and BROWSER_QA QA-E9 (line 430) stay valid. Spaced retrieval only works if the queue gets tapped daily; presentation is the lever.

3. **Bridge status board: per-domain mastery on the empty right side** · `OVERHAUL`
   Verified: since the shattered-station removal, showMenu's markup (starnix-shell.js lines 481-492) has no right-column element at all and .sx-bridge-left is capped at max-width:600px (CSS line 1467) — desktop widths show dead space over the nebula. Key sharpening: do NOT build a new renderer — _buildStatsSummary (~line 853) already renders per-domain mastery bars from questions.stats() with opts.compact and opts.maxDomains. Add a .sx-bridge-right 'Station systems' panel that calls it with {compact:true, maxDomains:6}, restyled as bridge subsystem readouts with the existing sx-dom-bar accents. Hide the panel under the existing @media (max-width:1000px) rule (CSS line 1498) so PLAYTEST A1's 1280x800-must-fit lesson holds. This is the menu's missing learning-efficacy signal.

4. **Shatter the real station art into sprite fragments** · `IMPROVE`
   Verified: drawShards (starnix-shell.js lines 333-336) still throws 46 procedural iris rects while the station beat now draws the real armStation sprite (drawStation, line 296) — the art vanishes and confetti appears. arm.js already has the exact proven technique at lines 1060-1073: a 3x3 grid of drawImage source-rect fragments (nat/GR cells) flying outward with per-fragment rotation and alpha fade. Port it into showCinematic as a 4x3 grid off stationA.img with pre-allocated per-fragment vx/vy/spin (01 §13), gated on stationA.ready with the current rects kept as the asset-not-ready/jsdom fallback. Add a BROWSER_QA entry — headless can't see this.

5. **Overhaul the Disruptor beam by porting ARM's layered fire moment** · `OVERHAUL`
   Verified: drawWarBeam (starnix-shell.js lines 307-332) draws the story's inciting incident as one stroked line plus a growing dot. Sharpened scope: don't design new layers — arm.js's in-game intro already implements the full treatment at lines 1036-1062: radial-gradient charge glow with a dashed aim line during the 1.0s charge, then a wide peach glow stroke with a bright white core stroke, a white impact flash, and scattering glowing cores. Port that composition into the shell beat, keeping the existing screen-shake (line 310) and the SFX_BEATS lasercharge/laserfire cues (lines 392-394) which already fire at charge start (B.beam+1.0) and fire (B.beam+2.0) — verify by ear that the lasercharge tail actually spans the 1.0s charge, lengthening it in audio.js if not. Reduced-motion keeps the flat line. BROWSER_QA entry required.

6. **First-run coach mark on the bridge menu** · `NEW`
   Verified: no onboarding exists anywhere in starnix-shell.js (no tour/firstMenuSeen/coach code; KBB's first-run tour in kbb.js per QA-G3(b) is the only precedent and is game-internal). A new player lands on showMenu (line 464) facing four strips, an exam divider, a rank strip and a daily dock with zero guidance. Add a one-shot pass gated by a persisted profile.firstMenuSeen flag (persist via core.persistence.save, same one-shot-latch pattern as rankSeen at lines 586-589): pulse the ARM strip (static outline under reduced motion, honoring the existing .sx-reduced class at line 474), plus a small dismissible tip ('New recruit? Start with Acropolis Rescue — it teaches the core loop'). One flag, one CSS class, one tip element; verify-build pins the flag latch; negative control removes the latch.

7. **Mission finale panel: accent-coded staggered reveal, launchable** · `IMPROVE`
   Verified: the .sx-mission panel (starnix-shell.js lines 198-207) fades in as one static opacity write (line 440), its three <li> spans render in flat grey (CSS lines 1420-1423 — var(--mid), no per-game accents), and NIT is absent. Stagger each line in ~0.4s apart synced to a soft tick sfx, color the em-dash game names with the matching GAME_META accents (iris/aqua/peach), add a fourth gold line for the Nutanix Interrogation Test, and make each line a real button that calls enterGame(id)/showExamSetup() directly — _clearScreen already cancels the cinematic RAF so the jump is clean. Turns the cinematic's ending into the first mission select. Reduced-motion shows all four lines at once.

8. **Letterboxed type-on captions with per-beat tick** · `IMPROVE`
   Verified: captions swap via one cap.textContent write per beat (starnix-shell.js line 441, lastCap guard) — flat for a cold open — and the vignette is two plain edge fills (lines 435-438). Render each caption character-by-character over ~0.6s (track a per-beat start time, write only when the substring length changes — extends the existing lastCap guard, no per-frame allocation), extend the vignette fills into proper letterbox bars behind the caption, and fire a soft UI tick at each caption start (add entries to SFX_BEATS, lines 392-399). Reduced-motion shows the full line instantly. Same type-on treatment the deferred D4 briefing wants, so structure it for later sharing.

9. **Fly kbbAsteroid art in the Kuiper belt beat** · `IMPROVE`
   Verified: drawWarpBelt (starnix-shell.js lines 341-365) renders the belt as 16 flat grey regPoly rocks (#2b2b3e fill, line 349), while assets.js ships all five kbbAsteroid1-5 sprites (confirmed present) that KBB already uses. Extend the existing cineImg loader (line 261) to the five asteroids, assign each entry in the pre-allocated rocks array (line 250) a sprite index at init, and drawImage with the current z-parallax drift/rotation, keeping regPoly as the asset-not-ready/jsdom fallback — the exact pattern drawStation/drawWarpBelt already use for armStation/bcmShip. Makes the belt beat visually continuous with the KBB game it foreshadows. BROWSER_QA entry.

10. **Daily dock: reset countdown and claimable badge** · `IMPROVE`
   Verified: the dock's compact daily strip (_renderDaily with {compact:true, head:false}, starnix-shell.js lines 494 and 600-639) gives no hint missions are date-seeded (D.ensure/prof.daily.date, line 607) and expire at midnight, and a done-but-unclaimed Claim button is easy to miss among compact rows. Add a small 'resets in Hh Mm' label next to the DAILY MISSIONS dock label (line 492), computed as time-to-local-midnight at render — no timer, refreshed on every showMenu rebuild — and a gold count badge on the label when any D.state(prof, idx) is done && !claimed. Pure presentation over existing StarNix.daily state; the claim wiring (lines 623-634) untouched.

## ARM Game (Acropolis Rescue Mission)

1. **Implement the lost-core resurfacing pool (it silently discards today)** · `FIX`
   VERIFIED real: arm.js:1413 carries the comment 'commit this attempt's lost cores to the (Increment-2) resurfacing pool' and line 1414 is just `sectorLost = []` — field-lost cores (pushed at lines 1220 and 2257) and depot-failed installs (stepDeposit ~1423-1431 only increments `dLost`, discarding the question id) vanish for the whole run. Spec 02 v1_4 line 83 explicitly marks pool consumption as the open Increment-2 item, and the §2 flow diagram promises 'resurfaces later'. This breaks the Leitner promise that missed material comes back. Wire `sectorLost` question ids (and depot failures) into a run-level `lostPool`; have `drawCoreQuestions()` (arm.js:758) draw 1-2 pool entries per sector first, marked with a distinct 'recovered core' visual; carry the pool through the G2 checkpoint snapshot in `saveCheckpoint()` (arm.js:1447-1454, which today saves only sector/coins/lvl/stationBuild/usedIds) so resume preserves it. Pin in arm-run.cjs: fail a core in sector N, assert its id reappears by sector N+2; negative control removes the pool draw.

2. **Give the four dreadnoughts distinct phases and signature attacks** · `OVERHAUL`
   VERIFIED: sectors 3/6/9/12 run byte-identical fights — `updateBoss` (arm.js:1291) does the same ±10% weave, same aimed shots from live weakpoints (1301-1307), same 5-8s seeker-missile cadence (1310-1317), same 60/40 beam/wall split in `updateBossLaser` (1364-1387, `runRng.next() < 0.6` at 1371). The ONLY per-boss variation is `BOSS_WP_HP_BY_TIER [6,9,12]` applied at line 881. Make the pattern table data-driven per boss index: B1 = current baseline (beam only, teach the fight); B2 adds the wall laser + periodic escort-drone spawns (reuse `spawnRoamer`, arm.js:896 — note it currently early-returns when bossActive suppresses spawns at 874, so gate that); B3 = alternating twin beams + tighter missile salvos; B4 finale = all weapons plus an enrage after the 3rd weakpoint (faster weave, shorter `laserCD`). One-line pre-brief callout in the boss `briefIntro` (arm.js:794). Extend the `setupBossSector` test seam (arm.js:3017, currently hardcodes sector = 3) to take a sector number and pin each boss's pattern table in arm-run.cjs.

3. **Add two roamer/guardian archetypes with tier-gated introduction** · `NEW`
   VERIFIED: there is exactly one enemy behavior — every enemy chases at capped speed 72 and fires an aimed 250-speed bullet (arm.js:2167-2175); roamers (spawnRoamer:896), guardians (spawnGuardians:903), waves (906) and the panic ring (915) differ only by `coreId` tether and `ENEMY_HP_BY_TIER` sponginess. Add an ORBITER (circles the player at ~240px standoff, faster fire cadence, ring/diamond silhouette in an aqua-shifted hue) and a LANCER (no gun; 0.6s telegraph then dash, higher ram damage than the current 18 at line 2244, chevron silhouette in peach). Shape+color double-coding satisfies 01 §12 colorblind rules. Introduce by `tierOf(sector)` (arm.js:66): T0 chasers only (unchanged onboarding), T1 mixes in orbiters, T2 adds lancers — fixing that sectors 9-12 currently only get spongier copies of sector 1's enemy. Add a `type` field to the spawn functions; keep the single update loop in updateSector allocation-free per 01 §13 (branch on type, no subclassing). Pin per-tier spawn mix in arm-run.cjs.

4. **Smooth the tier difficulty cliffs and add the spec'd difficulty-shape toggle** · `IMPROVE`
   VERIFIED: difficulty steps hard exactly twice — `tierOf` (arm.js:66) flips at sectors 5 and 9, so `ENEMY_HP_BY_TIER [1,2,3]` (line 63) doubles time-to-kill in one jump and `ENEMY_SHOT_DMG_BY_TIER [10,14,18]` (line 64) jumps with it; `bandFor` (780) and `BOSS_WP_HP_BY_TIER` (65) step on the same boundary. A player cruising sector 4 hits a wall in 5. Interpolate per sector instead: mix spawn populations (sector 5 spawns 60% 1-HP / 40% 2-HP, sector 6 30/70) and lerp shot damage across the 12 sectors — all four spawn sites (896/903/906/915) already read tier at spawn time, so a per-sector mix table is a contained change. Fold in the spec 02 v1_4 §3D 'difficulty-shape toggle' (line 131, still unchecked) as a row in `showSettings` (arm.js:2077-2096) next to the existing Reduced motion / Extra time toggles, persisted via `persistSetting`. Deterministic via `runRng`, so extend arm-run.cjs to pin the per-sector spawn mix table; negative control breaks the lerp.

5. **Fix the death penalty that strips your two best upgrades, and retune the coin curve to match** · `IMPROVE`
   SHARPENED — the original premise was inverted. Verified math: the full tree (5 upgrades x 8 tiers, `upCost = base + lvl*60`, bases 110-140, arm.js:2037-2039) costs ~13,440 coins, while a full-clear sector earns ~250 (comment at 2036) — 12 sectors banks ~3,000, so coins NEVER out-earn the tree and a post-MAX sink is moot. The real problems: (a) `showGameOverPanel` (arm.js:1500-1506) sorts your levels and deterministically strips -1 from your two HIGHEST upgrades — each worth up to 540 coins of spend, ~2 sectors of income, and it systematically flattens whatever build you invested in; switch to a coin fine or one random-among-owned level via `runRng`, and show the cost in the panel. (b) The late tree is unreachable by design — decide whether that's intended (spec 02 is silent); if not, add a modest per-sector income ramp (kill bounty at line 2190 is a flat 3, depot install a flat 25 at 1428) so late tiers come into reach exactly when tier-2 difficulty needs them. Pin the new penalty path in arm-run.cjs (die with a known lvl spread, assert the deterministic outcome under a seeded rng).

6. **Two new puzzle types from spec 02 §3D, rotated by tier** · `NEW`
   VERIFIED: the roster is six types (`PUZZLE_TYPES`, arm.js:81) drawn per sector by a flat `runRng.shuffle` at line 762 with zero tier gating, and spec 02 v1_4 line 92 explicitly wants 'mastermind decrypt / node-trace maze / timed debris' — none built. Build DECRYPT (4-symbol mastermind against the stability timer with feedback pips — a genuinely hard puzzle for T2) and TRACE (route a signal through a node maze, generated solvable the way rewire already validates). Gate by tier: T2 sectors weight the shuffle toward the harder end of the roster so puzzle difficulty finally has a curve to match combat's. Wire timer budgets into `PUZZLE_SECS`/`puzzleSecsFor` (arm.js:88-93, respecting the PUZZLE_MIN=10 floor and extra-time x1.5), and expose both through the existing `activePuzzle` probe/tapSolve seam (declared at line 154) so arm-run.cjs drives solve and breach paths headlessly. Negative control: break TRACE's solvability generator, confirm the new solvable-maze pin fails.

7. **Ship the deferred D4 briefing typing effect, harness-safely** · `NEW`
   VERIFIED open: CHANGELOG v0.112.0 records the deviation verbatim — the ~34ms/char CRT typing was deferred because it would race the A5 answer-lands-last DOM pin and arm-run's synchronous `briefText` reads; the known-open-threads list confirms it's still owed. Ship it with the race designed out: `setBriefMsg` (arm.js:843-853) sets the full text into `commsMsg` immediately (so pins and synchronous reads always see final text) and the reveal is purely visual — a JS-driven character mask or clip on a sibling overlay inside the CRT bezel. Console keys 1/2/3 (bound at arm.js:581-587) and any click skip to full; `.arm-reduce` and reduced-motion render instantly; the option keys in `commsOpts` enable only after reveal-complete OR skip. This is the last missing piece of the D4 center-console aesthetic (BROWSER_QA QA-D4) and a contained quick win. Negative control: strip the skip handler, confirm the new skip pin fails; verify the A5 pin still passes untouched.

8. **Ship's log: re-read this run's briefings from the pause menu** · `NEW`
   SHARPENED — concept verified (spec 02 v1_4 line 94: 'Codex / ship's-log to re-read briefings outside combat', unchecked; the shell Codex is stats-only), but the original detail cited a `q.briefing` field 'authored for all 233 questions' that does not exist: questions.js has ZERO briefing fields (226 `explanation` fields; `q.briefing || q.explanation` at arm.js:825 always falls through). The log should render what Vega actually said: the composed output of `teachLine()` (arm.js:820-828 — lead/body/close with the answer landing last per the A5 ruling). Add a LOG section to the pause panel built by `showSettings` (arm.js:2077), listing the current sector's five cores with topic tag, covered/collected/lost status, and the teach text, styled as center-console CRT pages to match D4. Learning efficacy is real: retrieval support right before a core scan or depot install, at zero new-content cost. Pin in arm-run.cjs: log lists 5 entries matching `drawCoreQuestions()` output; a collect updates the entry's status; the answer still lands last in each entry (extend the A5 pin, don't fight it).

9. **Sector visual identity: per-tier palettes and one seeded landmark per sector** · `IMPROVE`
   VERIFIED: all 12 sectors render the same field — `makeStars()` (arm.js:631-645) uses one fixed 4-color nebula array (line 640: iris/aqua/mantis/peach at fixed alphas) and identical star distribution, so sector 11 is pixel-cousin to sector 1. Shift nebula/star tint per tier (T0 iris-cool, T1 aqua-teal, T2 ember-peach warning tones from the doc-07 palette) and seed one non-colliding landmark per standard sector from `runRng`: a derelict hull (reuse the `armStation` sprite already loaded at SPR.station, line 288), a dense asteroid drift band, or a backdrop planet limb (`SPR.planet` already decoded for the intro dive, line 287). Draw in the far parallax layers (d=0.25/0.5) so readability and the v0.44 no-occluder feel ruling hold; skip under `reducedMotion` only if animated. Browser-blind by definition — add a QA-A14 entry to BROWSER_QA.md and reuse visual-playtest.mjs to shoot one frame per tier. Pin: landmark position deterministic per seed and clear of all core danger rings.

10. **Close the ARM browser/ear QA debt, including the C2 thruster-flame fix** · `FIX`
   SHARPENED — one claim corrected: QA-ARM-BOSS is already 'browser-VERIFIED still' (BROWSER_QA.md:250), so it drops out. What genuinely awaits eyes/ears: QA-ARM-MUS (line 257) and QA-ARM-SND (262) are explicitly EAR-BLIND (dubstep boss bed, missile launch sfx); QA-A13/A12/A11/A10 (missiles+wall laser, economy cadence, briefing rework, spread/assist) and QA-G5/G7 (save-resume for real, wall-laser safe column visible, belt-sweeper toast) have never been human-passed; QA-D3/D4 (cockpit HUD, center-console briefing) and QA-R1's radar-fix confirmation are also open. Plus PLAYTEST.md C2 (lines 68-69): the thruster flame is too small to sell the purchasable trail tints — the fix is the flame triangle in `drawShipAt` at arm.js:2349 (not ~2368), a contained ~1.5x scale change; note it only draws when `state === "SECTOR"`, so decide whether HOME flight should show it too. Bundle one session: run the checklist, fix C2 with a pin, log pass/fail per item in BROWSER_QA.md, file fallout as issues. Every other item on this list adds browser-blind surface, so clearing this debt first keeps the QA ledger honest per the CLAUDE.md hard rule.

## Chasm Chase

1. **Charge boost with correct answers instead of a flat gate count** · `IMPROVE`
   Boost currently fires every GATES_PER_BOOST=2 gates regardless of answer quality (_passGate, cc.js line 551 sets _boostPending on a plain modulo), so the run's biggest reward is uncoupled from learning. Change _boostPending to a streak meter: each correct gate answer adds charge (2 corrects = full boost; a wrong answer drains half), shown as a small HUD charge bar next to the shield pips in updateHud() (line ~2179). Knowledge literally becomes the ship's fuel — the core StarNix loop — with one new HUD bar. Sim-pure change in CCSim.answer() (line 608) / resumeAfterQuestion() (line 650), fully pinnable in cc-run.cjs; extend the G2 checkpoint snapshot (line 2084, which already carries gatesPassed and boostLeft) to carry the meter so resume preserves it.

2. **Make 90-degree turns a mechanic players actually see** · `FIX`
   TURN_KM is 250 (cc.js line 81) but boosts cover 100 of every 120 scored km (GATES_PER_BOOST=2 x GATE_KM=10, BOOST_KM=100), so ~5/6 of corners land mid-boost and are silently skipped by the overrun branch in step() (lines 331-334), autopiloted by _activateBoost() (line 661), or cancelled at boost-end (line 357). A player can play 20+ minutes and never fly one. Drop TURN_KM to ~30-40 AND re-anchor _nextTurnScore into the non-boost windows the way _nextGateScore already grid-snaps at boost end (line 361) — at TURN_KM 35 a single 100 km boost spans ~3 corners, so simply bumping by TURN_KM*1000 per skip is not enough; keep the existing +5 km off-grid offset (line 272) so a corner never lands on a question. Add the missing visual: a wall end-cap ahead plus a canyon-bend camera yaw in CCView — the _turnKickT ship flourish exists (lines 1672-1677) but the canyon itself never turns, so MOVE LEFT reads as arbitrary. Pin the new cadence and the one-corner-per-boost rule in cc-run.cjs and re-verify the G4 grace interactions (line 344).

3. **Milestone and personal-best moments in the HUD** · `NEW`
   Distance is the score (CCSim.score(), cc.js line 695) and prof.bests.CC is persisted at game over (line 2117), yet the run itself is silent — no celebration at 25/50/100 km and the player never sees their PB while running. Add: a milestone banner + spark burst + audio sting each 25 km; capture bests.CC in the mount-time persistence.load() callback (line 1906, which already stores garageProfile) and show 'PB 62.3 km' faintly under the km readout in updateHud(); when the live run crosses it, flash a NEW RECORD moment and swap the label. Cheap (updateHud + one timer), high retention value, and gives the crash screen's 'Run again' a target. Pin banner thresholds in cc-run.cjs; visuals go to BROWSER_QA.md.

4. **Give CC its own SFX vocabulary instead of borrowed generics** · `IMPROVE`
   The sim._emit hook (cc.js lines 1920-1926) maps only four events — coin to 'collect', gatePassed to 'click', powerup to 'correct', gameover to 'explode' — all shared generics, while 'boost' (line 666), 'turnwarn' (line 339), 'turn'/'turnfail' (line 347), and 'turnauto' (lines 358, 661) emit with no sound at all. The turn warning currently has zero audio channel (accessibility gap: the flashing cc-turn-banner is the only cue). Add dedicated synth events in audio.js sfxImpl() (line 998): a gate whoosh-and-freeze as the world pauses, a boost ignition ramp, a klaxon for turnwarn, a wall-clip crunch for turnfail/_onCrash, and the milestone sting from the PB item. Mirrors the v0.121-0.123 ARM missile/boss audio pass; ear-blind by definition, so log it in BROWSER_QA.md.

5. **Two new obstacle types: jump-duck chain and telegraphed rockfall** · `NEW`
   The spawner mix in _spawnRow (cc.js line 745) has been static since v0.56 (narrow/lowrock/arch/sweep + pinch/wall-extend/combo composites) and PLAYTEST.md's measured row mix (~line 110: narrow 26%, arch 17.9%, wall 15.8%...) confirms nothing new to read after ~2 minutes. Add (a) a CHAIN row: a low wall then an arch ~20 m apart forcing jump-into-duck rhythm (spacing must respect JUMP_TIME*speed; encode in _wouldHit at line 459 so cc-fairness-check.mjs proves solvability), and (b) ROCKFALL: a boulder that visibly drops from the rim onto a telegraphed lane (shadow decal + peach warning ring one full reaction window early), sealing that lane like OB_NARROW once landed. Both reuse existing pools (POOL_OBSTACLES=32) and M.rock; extend the fairness harness and the verify-build wall-contract pins before shipping.

6. **Render squeeze stretches as a continuous wall with an entry cue** · `FIX`
   The v0.102 C9 squeeze (scheduled in _maybeSpawn, cc.js lines 718-722; spawned per-row in _spawnRow lines 749-754) holds one side sealed for 1-2 km, but it is drawn as discrete 12 m bulge instances (_bulgeGeo Lz=12, line 1140) at row spacing (BASE_GAP 72 / MIN_GAP 54 m), so the 'canyon narrows' fiction reads as repeated identical rocks and the sealed lane can even look open between bulges. Add a dedicated long-wall visual: stretch/chain bulge instances (z-scale via the instance matrix) during a squeeze so the sealed side reads as one continuous promontory, plus a one-shot 'CANYON NARROWS' cue (banner + sfx) at squeeze start driven by a sim emit so it is headlessly pinnable. Collision stays identical (sim untouched except the emit); the look joins the CC section of BROWSER_QA.md.

7. **New power-gate buff kinds, including a study-time buff** · `IMPROVE`
   POWER_KINDS (cc.js line 147) has been the same five since v1.0 (magnet/invincible/shieldPlus/coinX2/slowmo) and coinX2 lost most meaning when coins became value-1 cells (v0.101 C12, line 96). Add: OVERSHIELD (a temporary 6th pip above SHIELDS_MAX absorbing one hit), SCHOLAR (next gate's question timer +50% — spends gameplay luck on learning room, stacking with the v0.126 1.5x window at line 575), and DOUBLE-DOWN (next gate pays +2 shields on correct, -3 on wrong — opt-in stakes). Implement in _grantBuff (line 672) / _passGate. Telegraphing the kind needs render work: power gates are ONE instanced mesh with a single gold material (gatePowMat, line 1050), so per-kind tint means either instanceColor on iGatePow or splitting per-kind meshes from POOL_GATES=8 — budget for that, or telegraph via the film color only. Pin grant/expiry in cc-run.cjs and extend buffStr (line 2483).

8. **Garage 2.0: deeper tiers plus a consumable cell sink** · `IMPROVE`
   GARAGE_ITEMS (cc.js line 2592) totals only 405 cells for a full build (50+120 hull, 75 boost, 60 magnet, 100 plating) — a handful of runs — after which cells are worthless and the coin pipeline is dead weight. Add tier 2-3s to boost/magnet/plating, two new upgrades tied to real mechanics (Corner thrusters: widen the turn grace window, synergizing with the TURN_KM fix; Focus capacitor: +25% question timer — a learning-support purchase), and a per-run consumables shelf (buy a one-run starting buff from POWER_KINDS at the crash screen — cheap, repeatable, infinite sink). garageState/garageBuy (lines 2600-2622) are DOM-free and already exported via CC.garage for harness pinning, so extend the existing pins in cc-run.cjs plus applyUpgrades() (line 479); the how-to loadout line (line 2267) picks new items up for free.

9. **Biome shifts: the canyon changes character every ~50 km** · `OVERHAUL`
   The run is one warm dusk canyon forever — FOG 0xB9885E hardcoded in the CCView constructor (cc.js line 949), one rock tint, one peak palette. Add 3-4 distance-keyed biomes (dusk sandstone, night bioluminescent, ice-blue, BCM industrial trench) as lerped fog/hemisphere-light colors plus material color tints on M.rock/floor/surface/peak (maps multiply by material.color, so no new textures). Three constraints the naive version misses: the end-cap plane is a separate fog-colored material (capMat 0xB9885E, line 1245) that must lerp in sync or the horizon shows a stale rectangle; scene.background is a static ccSky texture (line 951) that fights a night biome — dim it via scene.backgroundIntensity or crossfade to a color; and _peakMatNear/_peakMatFar colors are pinned by cc-view-smoke (line 1327), so those pins must become schedule-aware. Trigger off sim.scoreDistance with a 2-3 s crossfade plus an 'ENTERING: <biome>' banner reusing cc-turn-banner styling; pin the schedule headlessly in cc-view-smoke.mjs, look goes to BROWSER_QA.md.

10. **Squadron catch-up arc: overtake a BCM straggler at milestones** · `NEW`
   The fantasy is 'catch the squadron' but _buildSquadron (cc.js line 1397) holds 5 ships at a fixed uncatchable depth forever — zero payoff. Add a distance-keyed arc: every N scored km (e.g. 50) one straggler visibly falls back through the fog, grows, wobbles with damage sparks, and peels off past the camera; the HUD flashes 'SQUADRON: 4 REMAIN' and the sim emits a telemetry milestone. Almost all view-side — reuse the existing _flyT flyby machinery (lines 1769-1778) and spawnSparks (line 1822) — plus one sim counter derived from scoreDistance so it survives G2 resume for free. Pairs naturally with the 25 km milestone banners. Track the eyes-on pass in BROWSER_QA.md — this is the biggest 'why do I keep running' hook CC lacks.

## KBB Game (Kuiper Belt Battle)

1. **Add a post-battle debrief recapping the questions you missed** · `NEW`
   In-battle feedback shows the explanation once (kbb.js:2377, with the 120-word details-fold) and then it's gone; by the time the section map appears the miss is forgotten — weak retrieval reinforcement for the game's whole reason to exist. Collect wrong answers (question id, chosen option index, correct option) on run.battle in submitAnswer's wrong branch (kbb.js:561), and on entering the map (renderMap, kbb.js:2456) render a collapsible 'Debrief' card above the corridor listing each miss with its stem, the right answer, and the first line of the explanation. Pure view over engine data the battle already has; a perfect second exposure right after the error, complementing the Leitner loop. Pin in kbb-run that a wrong answer produces a debrief entry and a correct-only battle produces none; negative control severs the collection.

2. **Design and ship the unknown-stop event deck (closes the D6 open call)** · `NEW`
   Today an unknown '?' stop only calls claimCache() (kbb.js:751) for 12-25 coins rolled in genMap (kbb.js:2426) — the open D6 thread. Replace with an event table: salvage cache (coins, keep as one entry), distress signal (answer one difficulty-3 question: free consumable on correct, 4 HP hull scrape on wrong), drifting relic (free random common artifact, or coins if slots full), mender beacon (heal 10), BCM toll (pay 8 coins or +1 enemy intent next battle). Two placement notes the analyst missed: genMap and the map live in the VIEW layer with a view-side rng fork ('kbb-map:'+seed+':'+section, kbb.js:2418), so put the resolver in the engine next to claimCache with the event id rolled at genMap time and stored on the stop; and saveCheckpointKBB deep-copies run.map into the G2 checkpoint (kbb.js:2448), so event state must survive serialize/resume. Wire the map click handler at kbb.js:2532, pin determinism and each event's invariant in kbb-run, spec as 03 v1_3 with weights in CONFIG.

3. **Give the run an arc: BCM Flagship victory at section 3, then endless Deep Belt** · `OVERHAUL`
   The run is pure endless (spec 03 v1_2 section 1: 'Runs are endless and escalating') with SECTION_NAMES cycling 6 labels (kbb.js:2415), and PLAYTEST.md section E's 600-run sweep puts cohort mean cleared depth at 4.76 — most runs die mid-section-1 with no goal ever visible. Make section 3's boss the BCM Flagship: a named victory beat (win screen mirroring the loss overlay at kbb.js:2130-2146, telemetry 'run_ended' result:'win' alongside finalizeLoss's 'loss' at kbb.js:496-498, a persisted wins counter next to bests.KBB at kbb.js:2779), after which the player may 'push into the Deep Belt' and continue endless scoring exactly as today. Puts a winnable target ~15 rounds out — a strong study-session length — without touching locked balance curves. Needs 03 v1_3, and browser QA since the victory surface is new visual ground.

4. **Fix legendary unreachability with a boss-shop pity offer** · `FIX`
   PLAYTEST.md section E, verified verbatim: all four legendaries (metadata-ring, aegis-capacitor, golden-cache, singularity-seed) were bought ZERO times in the 600-run sweep — base price 24 (CONFIG.artifactPrice, kbb.js:48) plus pricePerSection scaling exceeds what runs ever hold, so a whole rarity tier is dead content, and the report explicitly names 'price curve or a pity offer' as the design lever left for ratification. Implement in buildShop (kbb.js:660): when entering the shop with run.round === CONFIG.roundsPerSection (the post-boss shop — winBattle at kbb.js:512 builds it before leaveShop increments), guarantee one unowned legendary among the offers at 30% off. Moves locked rarity distributions, so the kbb-balance targets need a documented re-lock in the same unit per the 'locked targets' rule.

5. **Expand boss mechanics from 3 to 6 and break the fixed repeat cycle** · `IMPROVE`
   BOSS_MECHANICS is exactly ['shielded','enrage','gated'] chosen by (section-1)%3 (kbb.js:61, 331) — spec 03 v1_2's '3+ mechanics, telegraphed' checklist (line 141) is technically met, but every section-4 boss is a deterministic rerun of section 1's, so a player past depth 3 never sees anything new. Add three: 'splitter' (at 50% HP spawns a second smaller HP bar that must also die), 'leech' (heals 15% max HP on each wrong answer — punishes guessing, rewards brace-and-think), 'jammer' (suppresses artifact modifyDamage hooks every other turn, telegraphed like shieldUp via the drawQuestion parity at kbb.js:490). From section 4 onward pick via run.rng instead of the modulo. Each needs: a new label in enemyName's mechanic-to-name map (kbb.js:314), an intent-chip telegraph in renderEnemy (kbb.js:2043-2049), kbb-run pins, and a negative control per mechanic.

6. **Add enemy intent patterns and roll them per-enemy instead of per-round** · `IMPROVE`
   ENEMY_PATTERNS is flat/ramp/alternating assigned by (round-1)%3 (kbb.js:62, 333), so round 1 of every section in every run is always 'flat' — regulars have zero read-the-enemy variety. Correction to the original pitch: the .kbb-intent.charge CSS class is NOT unused — renderEnemy applies it whenever current intent is 0 (kbb.js:2047, the alternating pattern's 'Charging' off-turn), and a 'skip then x2.5' pattern would near-duplicate alternating's existing 0-then-x2 toggle (currentIntent, kbb.js:346). Instead add 'siphon' (weak hit that also strips 4 shield in applyIncoming — makes brace-timing matter against it) and 'crescendo' (three-turn cycle weak/weak/heavy via a counter in advanceIntent, kbb.js:349) — both genuinely distinct reads. Assign the pattern via run.rng weighted by section in makeEnemy, keep bosses on mechanic-driven patterns, give each a distinct intent-chip text, and re-run the locked kbb-balance targets since incoming-damage distribution shifts.

7. **Restore consumable variety: add Overcharge and Stasis** · `IMPROVE`
   CONSUMABLE_IDS is down to repair/recharge/intel after the v0.98 K3 Purge cut (kbb.js:309), so consumableCap 4 rarely matters and one-click-repair's onConsumableUsed hook (kbb.js:132-134, +6 shield per use) has little to trigger on — the shop's consumable row (rollConsumableOffers, kbb.js:633) offers three near-interchangeable sustain items. Add 'overcharge' (next correct attack this battle gets +1.0 mult — a battle flag read in computeDamage's d.mult pipeline, kbb.js:426-433) and 'stasis' (enemy skips its next counter — set battle.skipEnemyCounter, the exact flag cold-tier already sets at kbb.js:213 and submitAnswer consumes at kbb.js:577). Wire both in useConsumable (kbb.js:731), single-battle flags, deterministic, cheap kbb-run pins (stasis = no enemyAttacked that turn; overcharge = damage delta), and the shop finally has offense/tempo choices next to sustain.

8. **Add artifact category resonance bonuses to reward themed builds** · `NEW`
   All ~40 artifacts carry a category (damage/sustain/defense/economy/utility/risk/scaling/domain — colored via CAT_COLOR, kbb.js:833, shown on the v0.127 fan cards' borders at kbb.js:2082-2084) but nothing rewards collecting within one: pairs are invisible. Add resonance in the engine, computed in single passes rather than per-artifact hooks: 2+ damage = +2 flat in computeDamage (kbb.js:427), 2+ defense = +3 shield in startBattle (kbb.js:478-480), 2+ economy = +1 coin in winBattle's coin pipeline (kbb.js:501-507), 2+ sustain = +1 healPower applied at repair resolution (kbb.js:553-557). Surface it on the fanned hand cards (renderArtifacts, kbb.js:2074): glowing category border + a 'RESONANCE' chip when a pair is live, so shop offers read as build decisions. Pins per bonus in kbb-run; balance note since it's a small power injection — verify against locked kbb-balance targets.

9. **Make the energy gem a real resource with surge turns** · `IMPROVE`
   The gem renders '1/1' hardcoded in buildHand (kbb.js:2169) and flips to '0/1' in onAnswer (kbb.js:2263) — its own comment admits it decorates 'today's real rule' of one play per turn. Promote to engine state (battle.energy/battle.maxEnergy) and give it play: a 3-correct streak (battle.correctStreak already tracked, kbb.js:539) charges a surge granting 2 energy next turn, letting the player take two actions on one correct answer (e.g. brace THEN attack). Add one rare artifact 'Twin Reactor' (surge at 2 streak). Deepens the v0.46 K5 attack/brace/repair agency without touching the answers-are-the-engine rule (a wrong answer still does nothing). The real work is sequencing in submitAnswer (kbb.js:528): the enemy counter (kbb.js:577-583) must fire after the LAST action of a surge turn, not between — pin that ordering in kbb-run and make the negative control break exactly it.

10. **Add between-run meta-progression: salvage currency and hangar unlocks** · `NEW`
   The only thing surviving a run is bests.KBB (read kbb.js:1182, written kbb.js:2779) — a dead run awards nothing, weak for a game built on repeated study sessions. On run end, bank ~10% of the run's earned coins as persistent 'salvage' via ctx.persistence (same p.saves/p.bests update pattern as saveCheckpointKBB, kbb.js:2435-2453). Spend it on a hangar screen: unlock a chosen starting common artifact, +5 starting max HP, or a 4th consumable slot. Seam note the analyst half-had: createRun already supports opts.preRunShop (kbb.js:777) and startDungeon (kbb.js:783) exists to exit it, but restart() deliberately bypasses the pre-run shop (kbb.js:2151, v0.68 J6) — so implement hangar picks as createRun opts applied to the starting squad, not a revived shop phase, and both fresh starts and restarts get them. Cap total spend so knowledge, not grind, still wins runs; spec as 03 v1_3 with a persistence schema note; kbb-run pins that salvage banks on loss and unlocks apply at createRun.

## Nutanix Interrogation Test

1. **Bank lint harness: shuffle-safe explanations and answer-pattern tells** · `FIX`
   shuffleOptions (exam.js:65-75) randomizes option order for learning integrity, but 3 shipped items still reference options by letter — actively wrong text after shuffling, an integrity bug in live content. Verified offenders in the generated questions.js: line 1117 ('Option C directly satisfies all requirements'), line 1548 ('The correct answers are B, C, and D… option C… option A… option E'), line 1581 (optionNote 'Similar to option A'). Add a bank-lint step to import-questions.mjs (or a bank-lint.mjs wired into npm run check): fail on letter-references in explanation/optionNotes, on positional options ('all of the above', 'both A and B'), and warn on craft tells (correct option consistently longest, duplicate stems). Fix the 3 offenders in starnix_questions.md (the source — questions.js is generated) by naming the option text instead of the letter. Negative control: plant a letter-reference, confirm the lint fails, remove it.

2. **Resolve the quarantine pile: 3 unverified keys, 4 missing explanations, a4q50 orphan** · `FIX`
   import-questions.mjs:20-24 hardcodes REVIEW_HOLD for a1q13/a1q27/a3q7 (key-unverified) and questions.side-report.json confirms 4 more held on 'schema: empty explanation' (a1q52/a1q59/a1q60/a2q55) — 233 parsed, 226 live, 7 authored questions dead. Do the adjudication workflow BACKLOG A2 promised: research each key against Nutanix docs, produce an evidence packet per item for Jason's sign-off, then promote or drop in starnix_questions.md; author the 4 missing @explain blocks the same way. Also settle a4q50: build.mjs exhibitBlock() inlines EVERY file in exhibit-images/ so a4q50.png ships (~50 KB dead weight per deploy) while its question block stays commented out in starnix_questions.md — wire it live or delete both (or make the inliner reference-driven). Learning-integrity rule holds: nothing ships without Jason's ratification, but the evidence packets and the plumbing are ready-to-start work.

3. **Persistent miss pile with a 'Redrill your misses' tile** · `NEW`
   The v0.87.0 redrill button (exam.js:740-741, wired at 774) only exists on the end screen — walk away and your misses evaporate; the Codex weakest-drill (_weakestQuestions, starnix-shell.js:908-921) approximates from Leitner bucket/streak/incorrect but isn't 'the exact questions you got wrong.' Record wrong answers into profile.missPile (id + missCount + lastMissedAt, capped ~60): completed runs can be captured shell-side in _recordExam (starnix-shell.js:746) since summarize()'s sum.wrong already carries the missed q objects — zero exam.js change for that path; add a hook in commit() (exam.js:598) if abandoned-run misses should count too. Remove an id after two consecutive corrects. Add a tile to showExamSetup next to the Exhibits tile (starnix-shell.js:694-702): 'Redrill your misses — N questions you've gotten wrong' launching the existing showExam(null, {questions, mode:'study'}) path. The single highest-leverage study tool the bank size already supports.

4. **Save/resume for Exam sim (G2 parity)** · `NEW`
   A 75-question sim is a 2-hour sitting (SIM_SECS_PER_Q=96, exam.js:280) and the only exits are finish or the quit button, which force-grades via submitSim(true) at exam.js:374 — close the tab and everything is gone, while all three games now save/resume (G2). Persist a profile.examResume blob on every draft change (question order by id, drafts, flags, remaining clock ms, mode/count/xt) and offer 'Resume your sim — 41 of 75 answered, 47:12 left' on the Testing station setup screen (showExamSetup, starnix-shell.js:644); clear it on submit or explicit discard. Order rebuilds from stored ids plus a stored option-perm seed replayed through shuffleOptions so grading indices stay valid. Pins: resume restores drafts/flags/clock exactly; negative control corrupts the blob and confirms the fresh-start fallback.

5. **Real post-sim report: per-question timing, full review, per-domain history** · `OVERHAUL`
   submitSim (exam.js:660-670) stamps every result timeMs:0, examHistory (starnix-shell.js:765-766) stores only mode/pct/correct/total/at, and the end screen reviews misses only (finish() renders sum.wrong exclusively, exam.js:736-764). Track time-on-question in sim (accumulate visible time per index in the RAF loop at exam.js:424-450), then upgrade finish(): pace stats (avg s/q vs the 96s budget, slowest 5 questions), a review-all toggle (correct answers with explanations too — reviewing rights consolidates), and store byDomain + avgSecs in each examHistory entry so the Codex readiness panel (_readiness, starnix-shell.js:926-941) can show per-domain trend across sims ('storage: 55% → 70% → 85%') instead of one opaque percentage. summarize() already computes byDomain (exam.js:78-88) — it is displayed once on the end screen and then discarded, never persisted.

6. **Sim review screen filters — flagged / blank / all (Pearson VUE parity)** · `IMPROVE`
   renderReview (exam.js:635-658) lists all 75 rows flat and the Submit button at exam.js:654 grades immediately with no blank-count warning; the real exam's review screen offers 'review flagged' and 'review incomplete' — exactly the workflow candidates must practice in the last 10 minutes. Add three filter chips above the row list (All N / Flagged N / Blank N) that filter the rendered rows, plus 'next flagged' navigation from within a question (extend the F-key handling at exam.js:690). Warn loudly on Submit when blanks remain ('3 unanswered — blanks score zero. Submit anyway?'), matching the real UI's confirmation. Small surface, big realism-per-line; pins on the filtered counts, browser-blind styling noted in BROWSER_QA.md.

7. **Blueprint-weighted domain sampling for Exam sim** · `NEW`
   run() builds the sim pool via uniform shuffle(pool, rng) + slice (exam.js:305-307) — a 75-question sim's domain mix is whatever the 233-question bank happens to be (vms-heavy), not what the NCP-MCI blueprint tests. The CHANGELOG B1 (v0.84.0) entry explicitly defers this: 'Blueprint-WEIGHTED domain sampling is deferred until Jason confirms the current official percentages.' Add a DOMAIN_WEIGHTS table to the question pack (import-questions.mjs emits it; Jason supplies the official section percentages), a pure sampleByWeights(pool, count, weights, rng) exported from exam.js for headless pins, and wire it into sim mode only (Study/Blitz stay uniform). Show the target mix on the setup screen so the realism is visible. Falls back to uniform when weights are absent, so it ships pipeline-first and activates the day Jason confirms the numbers — honoring the learning-integrity rule against inventing weights.

8. **Domain-targeted study: clickable Codex heatmap tiles + a domain lens on exam setup** · `NEW`
   The Codex heatmap (buildHeatmap, starnix-shell.js:996-1009) shows per-domain mastery with due badges, but tiles are inert innerHTML divs (lines 1002-1007) — you can see 'networking is weak, 12 due' and cannot act on it; the only targeted launches are the weakest-20 drill (line 1058) and the Exhibits tile. Make each tile a button launching Study on that domain's questions (filter core.questions.pool() by q.domain, reuse showExam(null, {questions, mode:'study'})), and add a matching optional domain picker row to showExamSetup (starnix-shell.js:644) so 'study just storage' is one tap from the Testing station too. Closes the loop the readiness composite opens: diagnose weak domain → drill weak domain, same screen.

9. **Daily Blitz gauntlet — one seeded 10-question run per day** · `NEW`
   Blitz has bests-per-length (profile.bests.EXAM, starnix-shell.js:784-792) and the combo chain (comboMult, exam.js:60) but no reason to return daily; meanwhile the daily-missions system (StarNix.daily, date-seeded templates, rendered via _renderDaily) already proves the pattern. Add a 'Daily gauntlet' tile to showExamSetup: 10 questions drawn with core.makeRng('blitz-daily:'+date) so every retry-tempted player gets the same set, one scored attempt per day recorded to profile.blitzDaily (streak + best), a calendar-streak chip on the tile, and a daily-mission hook ('complete the gauntlet'). Deterministic by construction, so the harness can pin same-date-same-questions and one-attempt enforcement; feeds the retention loop the Leitner scheduler needs to actually work.

10. **Study-mode notes: per-question 'why I missed this' memos** · `NEW`
   Study mode's graded view (paintGraded, exam.js:562-596) shows the authored explanation but gives the learner nowhere to externalize their own confusion — the single best-evidenced retention move after a miss. Add a collapsed 'Add a note' textarea under the feedback block in Study and in the post-sim review, persisted to profile.notes[qid] (plain text, capped ~500 chars, via the existing core.persistence), re-shown whenever that question reappears in any exam mode ('Your note from last time'). Surface a 'Noted questions (N)' tile on exam setup to drill exactly the items you annotated. Purely additive to authored content — zero learning-integrity exposure since notes are the user's own words, clearly labeled as such.

## Overall Flow / Integration

1. **Add a post-sortie debrief screen between game exit and the menu** · `NEW`
   Verified: Shell.prototype.exitGame (starnix-shell.js ~1362) tears down the module and calls showMenu directly — no summary exists. Correction to the original: there is no game-level onExit; per 01 §9a v1.2 the single canonical return hook is ctx.exit, routed through exitGame (the onExit at ~739 is the exam module's opts callback only). So: snapshot profile.xp, daily-mission progress (dailyMissionState), dueList size, and per-card Leitner buckets in enterGame (~1212); in exitGame, before showMenu, render a debrief panel — XP earned, cards promoted/demoted, missions completed with inline Claim buttons (reuse _renderDaily's claim wiring, ~600), achievements recap, new due count — with 'Fly again' / 'Bridge' CTAs. Wire the exam's onExit/onRetry path through the same panel. This closes the loop that makes cross-game XP feel like one campaign.

2. **Add a 'Today's flight plan' guidance card on the bridge** · `NEW`
   Verified: showMenu (starnix-shell.js 464+) shows the due chip (~554), dailies dock, and four equal strips, but never answers 'what should I do right now?', and carries a redundant top-bar Continue (~550) alongside the dock Continue CTA (~536-544). Add a pure planner in starnix-core.js (gate-pinnable) that ranks one next action from existing signals: core.mastery.dueList length, dailyMissionState of today's missions, weakest domain from core.questions.stats() byDomain, and sim recency from examHistory (_readiness ~926 already filters sims). Shell renders it as a single prominent CTA card: '14 reviews due — clear them first', then 'Daily: 3 more KBB correct — Launch KBB', then 'No sim in 9 days — run a 25-question sim'. Fold the redundant top-bar Continue away since the dock already carries it.

3. **Stop completed daily missions silently expiring unclaimed** · `FIX`
   Verified: ensureDaily (starnix-core.js ~446) regenerates profile.daily the moment the date key changes, discarding done-but-unclaimed missions — finish Sharpshooter at 11pm, close the tab, lose the XP. One caveat the original missed: the code comment reads '(unclaimed progress expires — daily)' and 01 §14 documents the daily system, so this reverses a documented choice — get Jason's ruling first per the 'ask, don't assume' rule, then bump 01 to v1_8. Implementation: before regenerating inside ensureDaily, walk the old day's missions via dailyMissionState and auto-bank done-and-unclaimed XP through claimDaily; toast 'Yesterday's missions paid out +90 XP' on the next menu render. Negative control: pin that an un-done mission still pays nothing. Pure date logic — pins cleanly in the existing daily determinism tests.

4. **Track a study-day streak with rewards** · `IMPROVE`
   Verified: no consecutive-days mechanic exists — profile.streaks/streaksBest (starnix-core.js ~220, ~736) are per-surface answer streaks only, and ensureDaily regenerates per date with no memory of yesterday. This wastes the strongest retention loop a daily-study app has. On rollover in ensureDaily, compare the outgoing daily.date to yesterday's dayKey and maintain profile.streakDays / streakBest (migrate-repair defaults like the v1.5 fields); show a flame chip beside the rank strip in _renderRank (starnix-shell.js ~569); add 7-day and 30-day achievements to ACH_LIST (~346); pay a small streak XP bonus on the first claimDaily of the day. Pure date logic, so it pins in the scheduler/daily determinism tests with a clean negative control (gap day resets to 1).

5. **Blueprint-weighted exam sims and per-domain readiness** · `IMPROVE`
   Verified open thread: exam.js mount (~304-305) fills sims by flat shuffle of the whole pool, so a 75-question sim over/under-samples domains vs the real NCP-MCI blueprint; no BLUEPRINT constant exists anywhere. Add a flat BLUEPRINT_WEIGHTS table (domain -> pct) in starnix-core.js beside RANKS (~290) — data-only, gate-pinned, values sourced from the official exam blueprint and quarantined if unverifiable per learning-integrity rules. Have showExam's sim path (starnix-shell.js ~709, launched from the Progress sim button at ~989 with showExam(75)) fill per-domain quotas from the pool before handing questions to the exam module. Extend _readiness (~926) and the domain heatmap (~995-1005) to show each domain's mastery against its blueprint weight, so 'weakest first' means weakest-where-it-costs-you-most.

6. **Surface due reviews beyond the bridge** · `IMPROVE`
   Verified with one correction: the tappable due chip lives only in showMenu (~554), though the Progress screen also shows a passive 'Due for review' stat (~861) and per-domain due badges — so the real gaps are the surfaces that pull the player back: the pause overlay (openPause ~1286: add '14 reviews waiting on the bridge' to the sx-pause-card), the title screen Start button (showTitle ~156: 'Start — 14 due'), and each game's end-of-run panel via the new debrief (or an additive ctx field — 01 §9a's freeze guarantee explicitly allows additive keys to grow). The Leitner queue is the pedagogical engine; it should be able to call the player back from anywhere, not just the one screen that already won their attention.

7. **Make Commander ranks pay concrete cross-game rewards** · `IMPROVE`
   Verified: RANKS (starnix-core.js ~290-302) are flat name/xp pairs; a promotion is one toast in _renderRank and nothing else, while all real unlocks (trail tints) key off domain mastery via COSMETICS/cosmeticUnlocked (~485+). One correction: drop the proposed 'Cadet unlocks Blitz' — Blitz already ships unlocked for everyone (exam.js ~276+) and retro-locking live content at a 150-XP rank is a regression. Keep additive-only rewards in a data-driven RANK_REWARDS table beside RANKS (gate-pinned like the thresholds): Pilot adds a bridge crest insignia, Lieutenant grants +25 starting KBB coins and an ARM shield cell via new additive ctx keys (01 §9a allows additive growth), Commodore appends a gold trail variant to COSMETICS with a rank-based unlock path parallel to the domain gate. Reward line renders in the promotion toast and the Codex.

8. **Make the station-rebuild fantasy persistent and visible everywhere** · `OVERHAUL`
   Verified: stationBuild is an ARM-run local (arm.js 158), incremented at line 1428, zeroed on new game (957), and the bridge's 'Station n/60' stat reads the transient save (starnix-shell.js 475/501: prof0.saves.ARM.stationBuild). The campaign fiction — rebuild the MCI Station — dies with every New game and never moves from KBB/CC/NIT play. Promote it to a profile-level meter fed from the mastery.record choke point (starnix-core.js ~195-215): profile.station = floor(masteredCount / 233 * 60), latched forever like trailsUnlocked so later decay never un-builds a module. Render staged reconstruction on the bridge — the armStation sprite re-lighting section by section (the menu vista slot is free since the v0.120.0 shattered-station removal, shell ~1489). Every surface then advances the same visible rebuild, not just ARM sorties.

9. **First-run onboarding order and bridge tour** · `IMPROVE`
   Verified: no profile.onboarded or coach-mark code exists anywhere in shell/core; a fresh profile lands on four equal mission strips and can launch a 75-question sim at 0 XP (Progress sim button at starnix-shell.js ~989; NIT tile routes to showExamSetup at ~531 with no guidance). Add profile.onboarded: on first menu visit, badge ARM as 'Start here — Operation 1', show recommended-order ribbons (ARM -> CC -> KBB -> NIT 'after ~50 cards seen', using profile.totals.questionsSeen, which the Scholar achievement at core ~361 already reads), and run a one-time 3-step coach-mark pass over the rank strip, daily dock, and due chip. No locks — just ordering — so it respects returning players and the existing card wiring in showMenu.

10. **Build the certification finale — an actual ending** · `NEW`
   Verified: ARM has a per-run 'Station restored' panel (arm.js sectorClear, ~1471-1475), but nothing marks the player exam-ready and no profile.certified exists — the game as a whole never ends. When the persistent station meter completes AND readiness is sustained (e.g. two sims >= 80 in profile.examHistory, which _readiness at shell ~926 already filters), fire a one-shot authored finale: reuse the showCinematic canvas machinery (~187) for a 'station relit' beat with the armStation sprite, then a certificate screen with rank, sim history, mastery stats, and date. Latch profile.certified so it replays from the Codex. Pairs naturally with the persistent-station item; together they give the whole Leitner grind a destination instead of an asymptote.

## Backend Programming

1. **Save-file safety net: export/import, last-known-good backup, flush on pagehide** · `IMPROVE`
   All progress lives in one localStorage key behind a 400ms debounce in makeLocalStorageProvider (starnix-core.js:766-811) — a tab close inside the debounce window silently loses the last answers (no pagehide/visibilitychange/beforeunload hook exists anywhere in the repo), and Apps Script's *.googleusercontent.com sandbox origin can rotate and wipe localStorage entirely. The load() path literally comments 'corrupt JSON -> fresh profile, never crash' — total loss by design. Fix in the provider: flush() on visibilitychange/pagehide; write a rotating 'starnix:profile:bak' copy before each overwrite and fall back to it when JSON.parse or migrate fails; add profile export-to-clipboard/file and import in the shell Settings screen via new persistence.export()/import() seams (starnix-shell.js has no export/backup UI today). Weeks of Leitner history are currently one bad write from gone.

2. **Gate the questions pipeline: bank-freshness --check mode + version-stamp consistency pin** · `IMPROVE`
   questions.js is generated from starnix_questions.md by import-questions.mjs, but the check script (package.json:13) never runs the importer — an edited source md ships stale until someone remembers 'npm run bank'. Add an importer --check mode (regenerate to scratchpad, byte-diff against committed questions.js, and diff the held list built from REVIEW_HOLD at import-questions.mjs:119 so a silently un-quarantined a1q13/a1q27/a3q7 fails loud) wired into npm run check. Same suite pins BUILD_VERSION/BUILD_DATE (starnix-core.js:27-28) against the CHANGELOG.md head entry — verified drifted right now: BUILD_DATE says 2026-07-03 while the [0.127.0] entry is dated 2026-07-04. Cheap to build, closes a real gate hole, and protects learning integrity (the quarantine list is currently enforced by nothing).

3. **Field error reporting: persisted error ring buffer + dev diagnostics screen** · `NEW`
   No window.onerror or unhandledrejection handler exists anywhere (verified: zero hits across build.mjs, starnix-core.js, starnix-shell.js) — the boot script catches only boot failures. The v0.116 ARM radar bug (CHANGELOG: undeclared i2 in drawRadarOnly threw ReferenceError 60x/s in real browsers from v0.111 on, while jsdom's null-context early-return kept every harness green) is the proof this class of bug ships. Add global handlers in starnix-core.js that append {msg, stack-head, build, screen, ts} to a capped (~20) ring persisted on the profile, plus a devMode diagnostics panel in starnix-shell.js (devMode gating already exists post-v0.125) showing build label, recent errors, and the telemetry tail — turning 'it felt weird yesterday' playtest reports into stack traces.

4. **Set up clasp deploy pipeline (make the 6.4MB paste obsolete)** · `NEW`
   Deploy today is copy/pasting the 6,436,394-byte index.html into the Apps Script editor — slow, error-prone, and untracked. Verified: no .clasp.json or appsscript.json in the repo, no deploy script in package.json, and CLAUDE.md's Deploy section says clasp is 'planned but not yet set up'. Add .clasp.json + appsscript.json, an npm 'deploy' script that runs the full gate (npm run check) then 'clasp push' + 'clasp deploy' with BUILD_VERSION from starnix-core.js:27 as the deployment description, and a post-deploy smoke that fetches the web-app URL and greps for the version stamp. Creates the audit trail of which build is live — needed because PLAYTEST.md feedback references versions — and unblocks item 9's server-side sync (which needs a Code.gs the paste workflow can't manage).

5. **Vendor Three.js and self-host Montserrat — drop both runtime CDN dependencies** · `IMPROVE`
   Two external points of failure in the 'single deployable file': THREE_CDN at build.mjs:14 (cdnjs three.js r128) and Google Fonts at build.mjs:90-92 PLUS a second runtime font injection at starnix-core.js:860 (fontLink to fonts.googleapis.com — the analyst missed this one; both must go). A CDN hiccup or corporate CSP kills Chasm Chase AND KBB's 3D combat path (kbb.js builds a Three scene too, e.g. the sprite billboards around kbb.js:1797-1807) plus the typography mid-study-session. Inline the pinned three.min.js into index.html as another script block with a build-time SHA check so the vendored copy can't drift, and add a font-family fallback stack plus optional base64 WOFF2 Montserrat subset. Update cc-view-smoke.mjs's r128 surface mock note if the vendored version ever moves. Sizes fold into the size-budget report item.

6. **Build size budget: compact questions.js output, per-module size report + gate assertion** · `IMPROVE`
   index.html is 6.4MB; build.mjs:119 prints only a single total-KB line and nothing asserts on it. Quick win: import-questions.mjs:140 writes questions.js with JSON.stringify(pack, null, 2) (354KB on disk; compact form saves roughly 40%). Extend build.mjs to print a per-module byte + gzip breakdown (core/shell/each game/audio/assets/questions/exhibits — assets.js alone is 2,689,756 bytes, and the report will show whether its base64 PNGs should be re-encoded WebP) and add a verify-build.mjs assertion that total and per-module sizes stay under declared budgets, so a bloated asset drop fails the gate like any other pin. BACKLOG.md's art-direction section already flagged bundle budget as a concern ('bundle is ~1.3 MB today' — it has since 5x'd with nobody watching).

7. **Make telemetry real: standardized answer events, persisted latency aggregates, readiness feed** · `OVERHAUL`
   makeTelemetry (starnix-core.js:874-889) buffers 500 events into console.debug and drops them — verified that exam.js and starnix-shell.js contain zero telemetry references, and nothing ever reads events() back. All four graded surfaces already funnel through mastery.record(id, correct, {game}) (arm.js:301, cc.js:631, kbb.js:535, exam.js:610/667), so emit one standardized answer event {qid, game, correct, latencyMs, timerPct, reason} from that choke point, persist per-question rolling latency/accuracy aggregates on the profile, and feed them into the Codex readiness/weakest-drill views: 'slow but correct' is a distinct study signal from 'fast and correct' that Leitner buckets alone cannot see. Callers must pass answer latency in the ctx arg (the timer state exists in each game's question overlay). Aggregates also become the evidence base for scheduler-tuning decisions.

8. **Scheduler tuning knobs + blueprint-weighted exam sim draw** · `NEW`
   INTERVALS/W/MASTERED_BUCKET (starnix-core.js:167-174) and TIMER (line 572) are plain consts — every tuning experiment is a code edit plus version bump. Expose them as a StarNix.tuning config object (defaults pinned by the gate, devMode overrides persisted on the profile for A/B feel testing), and extend makeQuestionProvider(pack, mastery) (starnix-core.js:593) with a domainWeights option so exam.js Sim form assembly (the draw around exam.js:69, which currently samples the bank without domain quotas) can match NCP-MCI blueprint percentages — the open 'blueprint domain percentages for weighted exam sims' thread. The domain taxonomy already exists end-to-end (DOMAINS in core, per-domain results breakdown at exam.js:723-727), so this is wiring, not modeling. Uniform draws over a 233-question bank over-test small domains relative to the real exam.

9. **Multi-device sync via Apps Script UserProperties behind the persistence seam** · `NEW`
   The app is served by Apps Script but never uses google.script.run (verified: zero hits repo-wide) — profiles are device-local, so studying on phone and laptop forks progress. Add a SyncProvider wrapping makeLocalStorageProvider (starnix-core.js:766): on boot, pull the server copy via google.script.run.getProfile(), reconcile by updatedAt (already stamped on every writeNow, line 778), push debounced snapshots to PropertiesService.getUserProperties (chunk/compress — 233 mastery entries can exceed the 9KB per-value limit), and feature-detect google.script so local dev, mock-core.js, and the jsdom harnesses keep the pure-localStorage path. Requires a server-side Code.gs in the Apps Script project, which the manual-paste deploy can't version — sequence this after the clasp item so getProfile/saveProfile ship from the repo. Pin the reconcile logic in verify-build.

10. **Performance budget harness: frame-time and allocation smoke in the gate** · `NEW`
   Spec 01 §13 mandates allocation-free update/draw loops, but the gate only proves structure — the drawCockpitHud per-frame string allocation shipped in v0.108-0.115 and was only caught by the hand-run 29-agent sweep (CHANGELOG [0.116.0]). Reuse the visual-playtest.mjs Playwright scaffold (chromium.launch already wired, line 18): run each game headed ~5s, sample rAF deltas and performance.memory growth (Chromium-only, which Playwright guarantees), assert p95 frame time and bounded heap growth per surface, as an opt-in gate step (PERF=1) so gate cost stays controlled. CORRECTION to the analyst's static-pin idea: a blanket 'no Math.random' grep would false-fail today — arm.js:1394/2649 and kbb.js:1797+ use it for documented cosmetic-only effects and exam.js:292 uses it as an rng default. Pin an explicit allowlist of those annotated call sites instead, so any NEW Math.random in a gameplay path fails the gate.

## Front End UI

1. **Keyboard focus overhaul: always-on focus ring, focus handoff on screen swaps, pause-overlay focus trap** · `OVERHAUL`
   Verified: the only :focus-visible rules in the whole build are kbb.js:1056 (.kbb-tile) and starnix-core.js:852 — and the core one is gated behind :root[data-contrast="high"], so normal-mode keyboard users get browser-default rings on a dark bg everywhere else (zero focus rules in starnix-shell.js, arm.js, cc.js, exam.js). Every screen swap (_clearScreen, starnix-shell.js:58-63, sets stage.textContent="") drops focus to <body> so Tab restarts from the document top. Fix: promote the core:852 aqua outline rule to always-on (drop the data-contrast gate — one-line change that covers sx-btn/sx-strip/sx-exam-opt/kbb tiles at once since it's a bare :focus-visible selector in core themeCSS); on each show* call move focus to the new screen's heading or first control; openPause (starnix-shell.js:1287) traps Tab inside .sx-pause-card and closePause restores focus to the Pause button. Pin with a verify-build assertion on the ungated rule + a jsdom focus-sequence test. This is 01 §12 keyboard support actually landing.

2. **Unify four parallel reduced-motion mechanisms into one data-motion attribute and close the residual gaps** · `FIX`
   Correction to the analyst's premise: the in-app toggle IS honored in most cited places — .kbb-reduced (kbb.js:1143/1165, from ctx.settings) kills the intent alert (914), .arm-reduce (arm.js:465) kills the huge-button pulse (3108), the rank pulse is JS-gated (starnix-shell.js:591), the exam combo pulse is JS-gated (exam.js:367). The REAL gaps: (1) showTitle (starnix-shell.js:156-169) never adds the sx-reduced class — only showMenu does (line 474) — so the title-screen bg-drift ignores the in-app toggle even though CSS line 1499 is ready for it; (2) kbb.js:903 .kbb-en-strike + .kbb-statline .final pulse are media-query-only with no .kbb-reduced twin; (3) exam.js:182 meter transition is media-only; (4) cc.js:2576/2580 turn-banner and boost-overlay animations are media-only with no in-app-toggle equivalent. Rather than patching four bespoke class systems, mirror _applyContrast (starnix-shell.js:143-150): set data-motion="reduced" on <html> at boot and on toggle flip, add [data-motion="reduced"] twins beside each prefers-reduced-motion rule, then retire sx-reduced/arm-reduce/kbb-reduced root-class plumbing. Negative control: flip the attribute in jsdom, assert twin selectors exist.

3. **Settings surface cleanup: gate Dev Jukebox behind devMode, lift Music style into Settings** · `FIX`
   Both halves verified. (1) showSettings (starnix-shell.js:1159+) unconditionally renders the 'Dev · Jukebox' seclabel (~line 1171) and calls _buildJukebox (line 1181) for every player, while v0.125.0 gated ARM's dev button behind devMode (arm.js:2067-2068, STARNIX_DEV || /[?&]dev\b/) — the shell has no devMode helper at all, so port that exact function and wrap the jukebox section. (2) The Upbeat/Chill genre toggle is built inline only inside openPause (starnix-shell.js:1301-1327, including the J3 lastGameId-track fix) — a player who never pauses can't find it. Extract the genre row into a _buildGenreRow builder alongside _buildVolumeSliders/_buildToggles and render it in both showSettings and openPause (in Settings the immediate playTrack retrigger is a no-op since the menu bed is playing — guard on self.lastGameId as the pause code already does). Both gate-pinnable in verify-build.

4. **Toast service upgrade: stacking queue, duration by length, dedupe** · `IMPROVE`
   Verified: _toast (starnix-shell.js:1391-1397) appends every toast at the identical absolute position (.sx-toast CSS line 1604: left:50%;bottom:28px) with a fixed 2200ms removal, and the achievement onUnlock loop (lines 129-131) calls _toast once per unlocked def — simultaneous unlocks render exactly on top of each other, and long strings (rank-up '✦ Promoted…', mission-complete at line 631) get the same 2.2s as short ones. Replace with a small queue: toasts stack vertically bottom-up, duration = clamp(1800 + 40ms/char, max ~6s), identical consecutive messages dedupe into one toast with a xN counter, reduced-motion skips any slide animation. Keep the sx-toast-gold variant (CSS 1614). ARM's separate .arm-toast (created arm.js:569) can stay game-local since it lives inside the game canvas chrome, but note it in the unit so the two don't drift further. Pin count/order via a jsdom multi-toast assertion in verify-build.

5. **Screen-reader pass on the exam/question surface: semantics + aria-live announcements** · `NEW`
   Verified: exam.js contains zero aria-* or role attributes (the only ARIA in the whole shell is aria-hidden on decor, aria-label on sliders at starnix-shell.js:809, aria-checked on toggles at 841-845). Options are bare <button class="sx-exam-opt"> elements built in the question renderer (~exam.js:463+) with no group semantics; grading (ok/bad classes, lines 128-129/222-227) and explanations appear silently; the Sim clock (.sx-exam-clockbox .ck, line 196) is visual-only. Add role=radiogroup around single-answer options (aria-pressed for multi-answer), 'Option A' prefixes in accessible names, one aria-live=polite region announcing 'Correct/Incorrect' + explanation summary and 'Question n of m' on navigation, and aria-live=assertive for the sim clock's final-minute warning. Give _toast (starnix-shell.js:1391) role=status so achievements/rank-ups announce. Pin the attribute strings in verify-build; the semantics are jsdom-assertable. NIT is where an SR-dependent studier would actually live — direct learning-efficacy win.

6. **Colorblind shape-cue audit across all pass/fail and telegraph states (01 §12)** · `IMPROVE`
   Verified color-only states: exam classic-skin option grading is border+background color only (exam.js:128-129 mantis vs peach; the station skin at 222-227 fills the .k dot but with the same shape for ok and sel — still no distinct glyph, and .bad has no marker); KBB's FINAL pulse (kbb.js:899) and intent alert (913) are peach-color cues; shell domain-bar tiers .sx-dom-fill.weak/mid/strong (starnix-shell.js:1531-1533), sim chips .sx-simchip vs .pass (1544-1545), and heatmap t1-t4 (1552-1555) are all hue-only; CC's low-time timer (cc.js:2358/2525-2526) swaps mantis→peach color only despite the comment claiming otherwise. Do one systematic pass: ✓/✗ glyphs inside graded option key-squares, an icon on KBB telegraph/final states, a pattern or tier-glyph on bars/heat tiles, a shape cue (e.g. pulsing border thickness) on cc-qtimer.low. QA method: run a deuteranopia/protanopia simulator over playtest-shots/ and log in BROWSER_QA.md. Note the settings key literally named 'colorblind' is labeled 'High contrast' (starnix-shell.js:834) — this pass closes the actual colorblind gap the setting name promises, plus BACKLOG §5 #12 P2.

7. **Shell small-screen/touch pass: sub-600px breakpoint, 44px touch targets, safe-area insets** · `IMPROVE`
   Verified: games have breakpoints (kbb.js:1107 @820px, arm.js:3298 @560px, exam.js:236 @820px) but the shell's only width query is @media (max-width:1000px){.sx-bridge-left{max-width:none;}} (starnix-shell.js:1498). .sx-back and .sx-pausebtn (lines 1590/1592) at padding:7px 14px + 13px font are ~32px tall — under the 44px touch minimum — inside a 48px .sx-gamebar (1588). viewport-fit=cover is set (build.mjs:87) but env(safe-area-inset-*) appears nowhere in the repo, so notched phones clip the gamebar and build badge. Add a ~600px breakpoint (stack .sx-bridge-topright, collapse the bridge dock rows, full-width strips), raise back/pause/skip/claim hit areas to 44px, and consume the insets on .sx-gamebar, the bridge dock, and .sx-build-badge. BACKLOG.md:146 '#15 touch + small-screen audit' is still open — this closes the shell half; log the phone-width look in BROWSER_QA.md since headless can't see layout.

8. **Tabular numerals + type-scale tokens for the non-exam HUD readouts** · `IMPROVE`
   Corrected premise: exam.js already applies font-variant-numeric:tabular-nums in 8 places (lines 102, 104, 117, 138, 144, 152, 196, 203 — score, combo, meter labels, results pct, stats, domain rows, sim clock, rail cells), so the analyst's 'only the settings slider' claim is false. The real gaps are everywhere else numbers tick: CC's distance readout .cc-dist (cc.js:2517) and coin/shield HUD, KBB's .kbb-statline HP/energy (kbb.js:907-908), the shell rank strip .sx-rank-xp (built at starnix-shell.js:584) and bridge counters .sx-dom-count/.sx-heat-pct (1528/1550), and ARM cockpit readouts — these all jitter horizontally as digits change. Apply tabular-nums to those, and while in the files, add a small type-scale as CSS vars in core themeCSS (starnix-core.js theme block) and raise the sub-11px micro-labels (kbb.js:869 .rt.tiny 10px, 882 card names 10.5px, 1017 rarity 10px, 1040 slot labels 10.5px) to an 11px floor with letter-spacing compensation. Mechanical, low-risk, pinnable in verify-build.

9. **Branded boot sequence with real inter-module progress, replacing the static 'Loading StarNix…' splash** · `NEW`
   Verified: index.html shows only #app:empty::after{content:"Loading StarNix…"} (build.mjs:99-102) while the multi-MB single file parses — a dead grey line as the game's first impression. Sharpened mechanism: Shell.boot runs only after everything parses, so it can't drive the splash — but build.mjs emits each module as a SEPARATE <script> block (blocks map, build.mjs:32), so the build can interleave tiny one-line status scripts between blocks ('Decoding assets…' before assets.js, 'Loading question bank…' before questions.js, 'Powering up the bridge…' before the shell) that update a styled pre-shell splash div with an NX-crest CSS animation — real progress, zero framework. Keep a reduced-motion static variant (@media only, since the profile isn't loaded yet), and route parse-time failures into the existing showError box (build.mjs:58-68) instead of hanging on the splash. Theme it as ship power-on: cheap personality plus a genuine perceived-performance win on Apps Script.

10. **Unify the four question-card implementations into one shared component layer** · `OVERHAUL`
   Verified — the same learn-loop surface is built and styled four separate times with drifted values: exam .sx-exam-card/.sx-exam-opt (exam.js:118-130 classic + 212-227 station skin, DOM at 463+), ARM's .arm-panel overlay (created arm.js:566, CSS 3214-3218, with per-state color classes), KBB's question zone inside its combat CSS (~kbb.js:851-1107), and CC's DOM qPanel/qTimer/qFeedback (cc.js:2441+, 2525+, with its own cont.focus() hack at 2105). Answer-button shape, confirm/lock-in behavior, timer presentation, and explanation layout all differ per game, so players re-learn the interaction per module — and any a11y fix (items 1/5 here) must land four times. Extract a core-owned builder (StarNix.ui.questionCard(host, q, opts)) plus one CSS block in core themeCSS with per-game accent vars; games keep their chrome and pass callbacks. Sequencing note: land this BEFORE the SR/colorblind passes if possible so those ship once — and budget for re-pinning, since verify-build asserts on the current per-game CSS strings and all four game harnesses exercise their own question flows.
